# LUN 변경 이력 (PR 기준)

> 저장소 머지된 PR 156건을 머지 날짜(KST) 순으로 정리. `npm run changelog`로 생성(마지막 생성 2026-09-10).

## 요약

| 월 | PR 수 | 주요 흐름 |
|---|---|---|
| 2026-06 | 71 | 재구축 초안 → 감사·보안 보강 → 역할 체계(교사·학생) → 배포 준비 |
| 2026-07 | 58 | 교육 기능(로직 카드·개념 배지·도감) → 코드 게이트 → 법적 문서 → 자동예시 |
| 2026-08 | 12 | 브랜드·SEO·공유 QR·학생 관리·운영 안정화 |
| 2026-09 | 15 | 법률 피드백 반영 → 게시판 개선 → 테스트 도입 → 품질 점검·게이트 확장 → 유형별 예시·제작 규칙 |

## 진행 중 PR

- **#157** [기능] feat: 생성 프로그램 품질 기준과 검증 파이프라인 강화 — 생성 프롬프트와 유형별 필수 동작·검증 규칙을 강화하고, 룰렛 모바일 규칙과 꾸미기 SVG 골격·조작 계약·유형 우선순위·랜덤 초기화 반복 검사를 반영했습니다.

## 2026-06

### 2026-06-16
- **#1** [기능] feat(board): 게시판 개선 — 중첩 카테고리(3단) + 좌측 패널 접기  
  게시판 카테고리를 평면 → 3단 중첩 트리(예: 2026 > 춘고 > 1반)로 확장했습니다.
- **#2** [수정] fix(audit): 보안·정합성·일관성 빠른 수정 + CI (점검 P2~P6)  
  2026-06-16 전체 감사 후속 — 빠른 수정 P2~P6. (치명 결함은 없었고, 정합성·일관성·배포준비 보강.) · rules(P3): `likeCount` 감소 음수 하한(`>0`). · rules(P4): `reports` create/update 분리 …
- **#3** [수정] fix(ci): Firebase Admin 지연 초기화로 무자격증명 빌드 통과  
  ## 문제 · PR #2 병합 후 main CI의 Production build 실패: `next build`의 라우트 page-data 수집이 `lib/firebase/admin.ts`를 import하는 것만으로 모듈로드 시점에 `serviceAccountKey…
- **#4** [기능] feat(survey): 선택지형 저학년 코딩 모드(/easy) — Survey-to-Prompt  
  타이핑 없이 선택지(클릭)로 프로그램을 설계하는 저학년·코딩취약계층용 모드(`/easy`).

### 2026-06-17
- **#5** [기능] feat(survey): 선택지 대폭 확장 + 연쇄(체인) 조건부  
  선택지형 저학년 코딩 모드의 선택지 가짓수·분기 깊이를 대폭 확장해 조합 다양성과 분기(알고리즘) 학습 효과를 높였습니다.
- **#6** [기능] feat(survey): 선택지형 모드 다듬기 — 생성 취소 · 'AI가 골라줘' · 결과 화면 고치기  
  선택지형(저학년) 모드를 세 가지로 개선합니다. · ### 1. 생성 중 '그만 만들기'(취소) · `requestGenerate`에 `AbortSignal` 추가. busy 화면에서 취소하면 진행 중 요청을 중단하고 설문으로 조용히 복귀(취소는 에러 토스트 없음…
- **#7** [테스트] test(dev): 설문 /api/generate 서버측 통합 점검(Admin SDK)  
  로그인이 필요한 서버 경로(`/api/generate`)를 브라우저·비밀번호·실계정 없이 검증하는 개발용 통합 점검 스크립트.
- **#8** [수정] fix(survey): 코드 점검에서 발견한 3건 수정  
  선택지형 모드 전체 점검(정적 리뷰 + 10개 설정 감사 + 실행 검증)에서 발견한 경미한 문제 3건 수정.
- **#9** [기능] feat(survey): 골라서 만들기 가이드 모달 (교육적 3장 안내)  
  `/easy`(골라서 만들기) 페이지에 교육적인 3장짜리 가이드 모달 추가.
- **#10** [기능] feat: LUN 메인 랜딩 페이지 + 서비스명 'LUN' 통일  
  서비스의 메인(랜딩) 페이지를 만들고 서비스명을 LUN(Logic Unfold Next)으로 통일.
- **#11** [기타] style: 다크모드 중립 팔레트로 재배색 (MUI 다크 참고)  
  다크모드 색조합을 MUI 다크 팔레트를 참고해 순수 중립(#121212 계열)으로 재배색.
- **#12** [기능] feat(landing): 메인 페이지 우클릭·드래그·선택 억제  
  메인(랜딩) 페이지에서 우클릭 메뉴·드래그·텍스트 선택을 억제. · ### 동작 · `contextmenu`(우클릭), `dragstart`(드래그) preventDefault

### 2026-06-18
- **#13** [기능] feat: 생성 품질 개선 — few-shot exemplar 라이브러리 + modify 모드 지시  
  Hermes Agent 분석에서 우리 코드젠 앱에 실효성 있는 2가지를 채택.
- **#14** [기타] style(ui): 글씨 크기 확대 · 헤더 LUN 통일 · 다크 명도 완화  
  사용자 피드백 반영 — 가독성 개선 묶음. · ### 글씨 크기 (작다 → 키움) · 헤더 네비 링크 `15 → 16.5px`
- **#15** [수정] fix(survey): 결과 화면 고치기(modify) 로딩 화면이 안 뜨던 문제  
  "골라서 만들기" 결과 화면에서 "이대로 고쳐줘!"(modify)를 눌러도 실행되지 않는 것처럼 보이던 버그.
- **#16** [수정] fix(a11y): reduced-motion에서 LUN 워드마크 투명해질 위험 차단  
  코드 리뷰(서브에이전트)에서 발견한 유일한 실질 항목. · ### 문제 · `.lun-shiny`의 `prefers-reduced-motion` 오버라이드가 `animation/background`만 끄고 `background-clip: text`는 안 풀어서, …
- **#17** [기능] feat(security): 좋아요·조회·포크 카운터 서버측 전환  
  배포 전 보안 항목 — 카운터 부풀리기 차단. ("숫자 세는 일을 학생→선생님(서버)으로 옮긴다") · ### 문제 · 좋아요/조회/포크 숫자를 클라가 직접 `increment`로 씀. 규칙으로는 같은 배치의 dedup 서브문서 생성을 확인할 수 없어, 로그인 사…
- **#18** [수정] fix(creator): 편집/이어만들기 작품 로드 실패 처리  
  배포 전 빠른 개선 #1. · `?edit=`/`?fork=` 로 Creator를 열 때 `getPost`가 실패(네트워크/권한)하면 unhandled promise rejection + 빈 Creator로 조용히 떨어졌음(왜 안 불러와졌는지 안내 없음). · `…
- **#19** [기능] feat(mypage): 내 작품 커서 페이지네이션(이전/다음)  
  배포 전 개선 #4. · ### 문제 · `fetchMyPosts`가 `limit(50)` 고정 → 작품 50개 초과 시 옛 작품을 마이페이지에서 접근 불가.
- **#20** [기능] feat(ui): window.confirm → 공용 확인 모달(useConfirm)  
  배포 전 개선 #2. · ### 문제 · 파괴적 동작(삭제) 확인에 브라우저 기본 `window.confirm`을 쓰고 있었음 — 디자인 통제 불가, 앱 톤(저학년 친화)과 이질적, 다크모드 미적용.
- **#21** [기능] feat(preview): 미리보기 무인증 쓰기 제거(옵션 C)  
  배포 전 개선 #2 (preview 무인증 POST). 메모 `preview-endpoint-deferred`의 옵션 C. · ### 문제 · 모든 미리보기가 `FullscreenFrame` 단일 통로로 코드를 `POST /api/preview`에 써서 임시 문서…
- **#22** [기능] feat(preview): 만료 previews 정리 cron (Vercel Cron)  
  배포 전 개선 #3 (previews cron 정리). 플랫폼 Vercel 확정 기준. · ### 문제 · `putPreview`의 기회적 청소(쓰기당 만료 20건)만 있어, 미리보기 생성이 한산한 시기엔 만료 문서가 `previews`에 쌓일 수 있음. (#2로…
- **#23** [문서] docs: 배포 체크리스트(Vercel + Firebase Spark)  
  배포 전 개선 #1 (env 주입) — 코드는 이미 전부 지원하므로(admin 지연초기화·`NEXT_PUBLIC_PREVIEW_ORIGIN`·`CRON_SECRET`·`.env.example`), 남은 산출물은 운영 가이드 한 건.

### 2026-06-19
- **#24** [기능] feat: 스트리밍 생성(라이브 코드+개념 내레이션)  
  배포 전 개선 #5 (스트리밍 생성). 스펙: `docs/superpowers/specs/2026-06-18-streaming-generation-design.md`, 계획: `docs/superpowers/plans/2026-06-18-streaming-gene…
- **#25** [수정] fix(creator): 스트리밍 대기 화면에 안내 문구 복원  
  생성기 스트리밍 중 코드 도착 전(thinking 단계) 화면에 BuilderBot만 보이고 안내 문구가 없던 문제 수정.
- **#26** [수정] fix(audit): 작성자명 사칭 차단 + 취소/미리보기·삭제 UX 보강  
  전체 감사(보안/에러/UX)에서 도출한 A 묶음(🔴 1·2·3 + 🟠 6·7) 일괄 수정.
- **#27** [기능] feat(generate): 취소 시 Gemini 호출 중단(AbortSignal 전파)  
  감사 🟠 #5. '그만 만들기' 취소가 한도 환불·UI 복귀는 했지만 Gemini 백엔드 생성은 다음 청크까지 계속되던 문제(특히 thinking 구간 취소 시 비용·리소스 지속). · ### 변경 · `AIProvider.generateStream(input,…
- **#28** [기능] feat: 닉네임 쿨다운 우회 차단 + easy 3단계 진행 체크리스트  
  감사 후속 2건. · ### 보안 — 닉네임 쿨다운 우회 차단 · `firestore.rules`의 users create/update가 `nicknameUpdatedAt is timestamp`만 요구해, SDK 직접 쓰기로 과거 시각을 적으면 15일 쿨다운을 …
- **#29** [수정] fix(a11y): busy 진행 announce + disabled 대비 + 코드 탭타깃  
  감사 🟡 3건 묶음. · ### 1. busy 진행 announce (스크린리더) · 생성기 진행 배너 → `role="status" aria-live="polite"`로 단계 문구가 낭독되게. 라이브 코드 영역은 `aria-hidden`(토큰마다 낭독되는 스팸…
- **#30** [수정] fix: 취소 판정 방어강화 + 마이페이지 삭제 시 stale 커서 폐기  
  재점검(코드 리뷰) 후속 — 선택적 미세 강화 2건. · ### 1. 서버 취소 판정 방어강화 · `/api/generate` 스트림의 abort 판정에 `e.name === 'AbortError'`를 OR로 추가. 기존 `req.signal.aborted`가 1…
- **#31** [기능] feat(landing): 메인 스크롤 경험 + 숨은 회사 푸터  
  메인 랜딩을 "묵직한 스크롤 → LUN 확대/페이드 → 숨은 회사 푸터" 경험으로 개편.
- **#32** [기능] feat(auth): 이메일 인증 요구(생성+게시판 쓰기)  
  무분별한 회원가입(가짜 이메일) 차단. 이메일/비밀번호 가입은 인증 없이 즉시 사용 가능했음 → 계정 양산으로 일일 생성 한도(30/계정) 우회·게시판 도배 가능. Google 로그인은 자동 인증, 관리자는 예외. · ### 변경 (차단 범위 A: 생성 + 게시판…

### 2026-06-20
- **#33** [기능] feat(account): 회원탈퇴(본인 계정 삭제)  
  로그인한 일반 회원이 마이페이지에서 본인 계정과 모든 작품을 영구 삭제할 수 있게 한다.
- **#34** [기능] feat(teacher): 선생님 역할 체계(B) — 관리자가 선생님 발급·관리  
  역할 체계 분해 A→B→C 중 B. 관리자가 선생님(teacher) 계정을 발급·관리하고, 발급된 선생님이 이메일 인증 없이 로그인·생성·게시할 수 있게 한다. 학생·풀 차감 카운팅·게시판은 C(범위 밖). · ### 변경 · 역할 = `teacher` custo…
- **#35** [기능] feat(student): 학생 계정+한도+공유 풀 차감(C1)  
  역할 체계 A→B→C 중 C1. 선생님이 학생 계정을 반 단위로 발급하고 학생별 한도(1일/총)를 정하면, 학생이 일반 앱을 쓰되 자기 한도 + 선생님 공유 풀에 제약된다. 선생님 게시판은 C2(범위 밖). · ### 변경 · `student` claim + `s…

### 2026-06-21
- **#36** [기능] feat(board): 선생님 게시판(C2) — 역할 체계 완성  
  역할 체계 A→B→C 마지막 조각. 각 선생님에게 자기 이름의 게시판을 주고, 학생 작품이 자동으로 그 게시판에 올라가며, 선생님이 부적절한 글을 삭제(모더레이션)할 수 있게 한다. · ### 변경 · 게시판 모델: `Category.teacherUid?` + `…
- **#37** [수정] fix(admin/auth): 선생님 풀 사용량 표시 + 로그아웃 토스트 제거  
  테스트 중 발견한 두 가지 UX 수정. · ### 1) 관리자 선생님 목록에 사용량 표시 · `/admin/teachers`가 총 한도만 보이고 소비량을 안 보여줬음 → `GET /api/admin/teachers` 응답·`Teacher` 타입·목록 UI에 `us…
- **#38** [수정] fix(admin): 없는 uid로 선생님 PATCH/DELETE 시 500→404  
  code_review.txt 2-2 반영. `app/api/admin/teachers/[uid]/route.ts`의 `ensureTeacher`가 `adminAuth.getUser`를 try 밖에서 호출해, 존재하지 않는 uid를 PATCH/DELETE하면 use…
- **#39** [기능] feat(account): 탈퇴 시 본인 글 신고 청소(내가 낸 신고는 보존)  
  code_review.txt 1-1 반영 — 단, 사용자 요청대로 구분 적용.
- **#40** [수정] fix(board): C2 하드닝 3종(트랜잭션·신고청소·업로드 가드)  
  code_review.txt(C2) 3개 항목 반영. · ### 1) 학생 업로드 버튼 가드 · 게시판(`getMyBoard`) 로드 전 "올리기"를 누르면 에러를 보던 문제 → board 확보 전 버튼 비활성화("게시판 확인 중…"). 비학생 흐름 불변.
- **#41** [기능] feat(teacher/board): 발급 목록 복사·저장 + 업로드 로딩 지연 안내  
  code_review.txt 2차 보고서의 2·3 반영(1번은 이미 처리돼 제외).
- **#42** [기능] feat(teacher): 게시판 최근 50개 제한 + prefix 입력 검증  
  code_review.txt 3차 보고서의 2·3 반영(1번은 인덱스 이미 존재 — 코드 정상이라 제외).

### 2026-06-22
- **#43** [기능] feat(sound): 선택 피드백 사운드 + 성공 효과  
  저학년 "장난감 같은 즐거움"을 위해 선택·성공 순간에 가벼운 소리 + 톡 효과(PRODUCT.md "모션은 반응이다"의 청각판).
- **#44** [수정] fix(board): 목록 액션 버튼 44px 탭타깃  
  감사 C2(게시판 액션 버튼 36px) 중 크기만 반영(호버 노출은 사용자 결정으로 유지).

### 2026-06-24
- **#45** [기능] feat(teacher): 교사 신고 인박스 (우리 반 신고 보기·삭제·무시)  
  ## 무엇을 · 신고된 콘텐츠를 우리 반 교사가 직접 처리할 수 있는 인박스를 `/teacher` 콘솔에 추가한다.
- **#46** [기능] feat(rules): categoryId·신고 정합성 검증 (위조·무단투입 차단)  
  ## 무엇을 · 클라이언트가 임의로 채워 보내던 두 필드를 firestore.rules의 교차문서 검증(get/exists)으로 강제.
- **#47** [기능] feat(auth): 학생 간편 로그인 (학교+학번+PIN) + 단일 세션  
  ## 무엇을 · 초등 저학년이 긴 이메일·비번 대신 학교(드롭다운) + 학번 + 6자리 PIN으로 로그인.
- **#48** [성능] perf: authedFetch 통합 + 페이지네이션 + N+1 배치 (4단계 확장 대비)  
  ## 무엇을 · 동작을 보존하며 확장에 대비: (A) 클라 인증 fetch 중복 제거, (B) 전체-컬렉션 읽기 → 페이지네이션, (C) N+1 루프 → 배치.
- **#49** [수정] fix(admin): 교사 삭제 시 schools 문서 정리  
  ## 무엇을 · 교사(학교) 계정을 삭제하면 `schools/{schoolCode}` 공개 문서가 남아 삭제된 학교가 학생 로그인 드롭다운에 잔존하던 문제 수정.

### 2026-06-25
- **#50** [수정] fix: 감사 후속(admin/users 견고화 + window.alert/prompt 제거)  
  배포 전 전체 점검에서 나온 🔴 항목 중 실질 2건 수정. (점검 항목 #2 "관리자 신고 카드 중복"은 오경보 — `groupReports`가 누적 배열을 postId로 그룹핑해 중복 없음.) · ## #3 admin/users usage 조인 견고화 · `a…
- **#51** [기타] harden: 신고 필드 검증·createdAt 상한 + 미리보기 격리 명시적 실패  
  취약점 재점검(적대적 3갈래)에서 나온 🟢 저비용·확실 항목 3건. (핵심 보안은 견고 확인 — 심각·XSS·권한상승 0.) · ## 1. 신고 필드 검증 (rules) · `reports` create/update에 `postTitle`(≤120)·`postA…
- **#52** [기능] feat(landing): 무조작 시 네비 라벨 노출로 저학년 진입 안내 (+ DESIGN.md 동기화)  
  ## 배경 · 출시 전 디자인 점검(impeccable critique, 30/40)에서 가장 큰 약점은 인트로 랜딩이 주 사용자(초등 저학년)에게 첫 행동을 안내하지 못함이었음.
- **#53** [기능] feat: 만들기 폼 임시저장 + 생성 실패 문구 친화 (출시 안정성)  
  출시 전 재점검에서 3점에 머물던 두 항목(#5 오류 예방, #9 오류 복구)을 코드로 확인 후 보강.
- **#54** [기능] feat: 라우트 진입 전환(C-lite) + 푸터 LUN 반짝임 감속  
  메인↔기능 페이지의 톤 격차 체감을 줄이기 위한 전환 폴리시 + 푸터 모션 미세조정.
- **#55** [문서] docs: CLAUDE.md 현 아키텍처로 갱신  
  역할체계·학생로그인·서버게이트(requireAdmin/Teacher instanceof)·authedFetch·정합성 규칙(categoryAllowed/postOwnerMatches/notImpersonating)·쿼터(교사·admin 100)·self-test …
- **#56** [수정] fix(motion): page-enter 견고화 (both→forwards)  
  라우트 진입 전환 `.page-enter`가 `animation-fill-mode: both`라, 애니메이션이 어떤 이유로든 실행되지 않으면(드문 엔진·확장으로 비활성 등) 모든 비-랜딩 콘텐츠가 `opacity:0` 상태에 갇혀 빈 화면이 될 수 있었음.
- **#57** [기능] feat(prompt): 생성 시스템 프롬프트에 디자인·접근성·내용안전 계약 주입  
  Gemini 코드생성 시스템 프롬프트(`lib/ai/prompts.ts`) 적대적 점검 후속.
- **#58** [수정] fix(prompt): 디자인 계약 강화 + exemplar 우선순위 명시  
  2차 적대적 점검 후속. 1차 점검에서 적용한 계약을 다시 공격해 나온 항목들 반영. · ## KID_CONTRACT 강화 (1·2·3) · 1. 밝고 생기 있는 색감 단서 복구 — 직전 PR에서 `KID_CONTRACT`가 기존 "색은 밝고 친근하게"를 통째 교…
- **#59** [수정] fix(prompt): 수정 모드 충돌 해소 (survey 확장지시 ↔ 최소수정, 계약 범위)  
  modify 모드를 적대적으로 재점검(앞선 2패스에서 얕게 봤던 부분)한 결과 반영.

### 2026-06-26
- **#60** [기능] feat(board): 교실 보드 비공개화 (Plan 1)  
  게시판 리뉴얼 Plan 1 — 교사(교실) 보드를 "그 교사 + 그 반 학생 + admin"만 읽도록 비공개화.
- **#61** [기능] feat(share): 공유링크 관람 PIN (Plan 2)  
  게시판 리뉴얼 Plan 2 — 교실(비공개) 게시물을 외부(부모)가 공유 링크 + 반 공용 관람 PIN으로 보기 전용 열람.

### 2026-06-29
- **#62** [기능] feat(photo): 사진 업로드 — 멀티모달 활용 + 토큰 치환(교실 한정)  
  학생/교사가 사진 1장을 올려 Gemini(멀티모달)가 그 사진을 활용한 프로그램(퍼즐·필터·스티커·사진 게임·꾸미기)을 만들고 교실 보드에 게시·공유.
- **#63** [수정] fix(photo): 미리보기 사진 치환을 서빙 시점으로 이동 (previews 1MB 오버플로 해소)  
  ## Summary · `__PHOTO__`를 여러 번 참조하는 생성 코드(예: 9칸 DOM 직소퍼즐에서 칸마다 `url(__PHOTO__)`)의 미리보기가 `previews` 문서 Firestore 1MB 한계를 넘겨 `putPreview`가 throw → 미리…
- **#64** [기타] polish(photo): 공유버튼 라벨·imageCompress 정리·옛 주석  
  사진 기능 소소한 폴리시(비차단): · PostPreview: 교실 글의 "공유 링크 복사" → "관람 링크 복사"(앱 내 "링크 복사"(보드 URL·멤버용)와 외부 "관람 PIN 링크"를 라벨로 구분, 관람 PIN 용어와 정합).
- **#65** [문서] docs(comments): 급소 결정 의도 주석 — 리뷰 오해 방지  
  외부 적대적 리뷰(code_review.txt)가 오해한 지점 = 의도가 코드에 안 적혀 있던 지점.
- **#66** [수정] fix(teacher,counts): 학생 시작번호(전학생 추가) + 조회수 increment 전환  
  code_review 2차에서 검증 후 수용한 2건(코드로 사실 확인된 것만).
- **#67** [수정] fix(counts): view 증가 실패 시 롤백 — 영구 미집계 회귀 수정  
  자체점검(+독립 리뷰 교차확인)에서 발견한, 직전 PR #66이 넣은 회귀를 수정.
- **#68** [수정] fix(rules): createdAt 미래조작(상단 영구고정) 차단 [#3 수용]  
  code_review 3차에서 검증 후 수용한 1건(+문서화 2건). · ### #3 (수용) — createdAt 미래조작 상단 영구고정 차단 · `validPost`가 `createdAt is number`만 검사 → 로그인한 누구나 SDK로 `createdA…
- **#69** [수정] fix: 사진 캡 350k 정렬 + 글 삭제 서버일원화(신고 캐스케이드) [#1·#3]  
  code_review 4차에서 검증 후 수용한 2건 (#2는 반려). · ### #1 (수용) — 사진 캡 비대칭 → 쿼터 증발 · generate/preview는 사진 400k 허용인데 저장 rule(`validPost photo<=350000`)은 350k →…

### 2026-06-30
- **#70** [수정] fix: 글/신고 삭제 원자화(같은 batch) [#2]  
  code_review 5차에서 검증 후 수용한 1건 (#1·#3 반려). · ### #2 (수용) — 삭제 원자성 붕괴 · 삭제 라우트 3종이 `await postRef.delete()` 후 별도 batch로 신고를 지워, 그 찰나에 크래시·네트워크 오류가 나면 …
- **#71** [문서] docs(comments): fork 펌핑 = 알려진 경미 어뷰징(수용) 주석 [#2]  
  code_review 6차 결과 — 출시 블로커 0. 2건 모두 처리: · ### #2 (수용·문서화) — fork 카운트 펌핑 · fork만 1인1회 dedup이 없어 직접 API 반복호출로 `forkCount`를 부풀릴 수 있음. 단 쿼터·보안·정합성·자동랭킹…

## 2026-07

### 2026-07-02
- **#72** [기능] feat(security): 응답 보안 헤더 (클릭재킹·CSP·nosniff)  
  외부 취약점 분석 #1 — 배포된 앱에 보안 헤더가 없어서 추가. · ## 추가한 헤더 (`next.config.ts`) · 전역: `X-Content-Type-Options: nosniff` · `Referrer-Policy: strict-origin-when-…
- **#73** [기능] feat(security): CSP 강제 승격 + favicon  
  외부 취약점 #1 마무리 — CSP를 Report-Only → 강제로 승격.
- **#74** [기능] feat(security): Firebase App Check (reCAPTCHA v3) [#2]  
  외부 취약점 #2 — App Check로 "우리 앱에서 온 요청만 통과" 계층 추가.
- **#75** [기능] feat(security): App Check debug token (로컬 dev/Enforce 대비)  
  #2 후속 — Firestore App Check Enforce를 켜기 전 로컬 개발이 안 막히게 debug token 지원.
- **#76** [수정] fix(security): 미리보기 치환 메모리 폭증 DoS 상한 + 공유 레이트리밋 신뢰 IP [7차 #1·#2]  
  code_review 7차에서 검증 후 수용한 2건 (#3 단일세션은 쿼터바운드로 반려).
- **#77** [수정] fix(security): 남용/증폭 벡터 4종 하드닝 (B2·B3·B4·B5)  
  공격적 취약점 재점검(규칙 심층 감사 + 비즈니스로직/레이스/남용/정보노출 감사)에서 나온 실증 가능한 남용·증폭·정보노출 벡터 4종을 수정.
- **#78** [운영] chore: 코드점검 보고서 Batch 2 (저위험 정리/문서)  
  코드점검 보고서(2026-07-02)의 저위험/문서 항목을 정리. 기능 영향 최소, 대부분 주석·문서. · ## 변경 · #18 `genRate`/`shareAttempts` 주석 정정 — "슬라이딩 윈도" → 실제는 고정 윈도(경계에서 최대 2x 버스트 가능하나…
- **#79** [수정] fix: 코드점검 보고서 Batch 1 (실사용 버그/기능)  
  코드점검 보고서(2026-07-02)의 실사용자 영향 버그 + 기능/비용/보안 항목.
- **#80** [수정] fix(auth): 로그인 실패 메시지 개선 (App Check/네트워크 차단 안내)  
  Auth App Check 강제 후, reCAPTCHA 토큰을 못 얻으면(학교·회사 방화벽이 Google/reCAPTCHA 차단, 광고차단 확장, 네트워크 불안정) 로그인이 막힌다.
- **#81** [수정] fix: 서브컬렉션 고아 정리(#3) + 미리보기 레이트리밋(#10)  
  코드점검 보고서의 #3(리소스 누수)·#10(비용/남용) 수정. · ## 변경 · #3 글 삭제 시 likes/views 서브컬렉션 고아화 — Firestore는 부모(post) 삭제 시 서브컬렉션을 자동삭제하지 않아, 인기글을 지우면 `posts/{id}/lik…
- **#82** [수정] fix(moderation): 호모글리프/전각 사칭·비속어 우회 차단 (#2-A)  
  사칭(#2) 대응 옵션 A — 클라 정규화 강화. 코드 확인 결과 정상 UI에선 사칭이 대부분 이미 막혀 있었고(authorName=본인 닉네임 고정·닉네임 유일성 claim·공백/기호/zero-width 예약어 차단), 유일한 실제 갭은 호모글리프였음. · ##…
- **#83** [수정] fix: 교사보드 딥링크 레이스(#8) + requireAdmin checkRevoked(#19)  
  코드점검 저순위 후속 중 #8·#19 처리(#14는 반려). · ## 변경 · #8 교사보드 딥링크 콜드로드 레이스 (`components/board/BoardView.tsx`) — 콜드 `/board?post=<교실글>` 진입 시 `categoriesRef`가 …
- **#84** [수정] fix(security): 교사 checkRevoked(M1) + count 레이트리밋(M3) + 카테고리 삭제 캐스케이드(M5)  
  보안점검 재감사(2026-07-02)의 Medium 중 코드로 고칠 수 있는 M1·M3·M5 처리.
- **#85** [수정] fix(audit): Low 정리 — L4 PIN강화·L6 env·L8 환불day·L9/L10 문서화  
  보안점검 재감사 Low 항목 중 사용자 결정분 처리(L4·L6·L8 개선, L9·L10 문서화).

### 2026-07-03
- **#86** [기능] feat(stats): 일일 운영 통계 — 방문 비콘 + 집계 엔드포인트  
  매일 운영 리포트(가입자·생성호출·방문자·신고·게시)를 Claude 루틴이 받아볼 수 있도록 데이터 수집·집계.
- **#87** [기능] feat(stats): daily-stats 주간 모드(?period=week)  
  주간 트렌드 루틴을 위해 `/api/cron/daily-stats`에 `?period=week` 추가.
- **#88** [수정] fix(deps): form-data High 취약점 해소 (npm audit fix)  
  주간 의존성 보안 감사 루틴(`lun-dep-audit`)이 탐지한 High 1건 수정.

### 2026-07-06
- **#89** [기능] feat(stats): Gemini 토큰 실측 로깅 + 비용 리포트  
  생성 건당 실제 Gemini 비용을 측정해 주간 루틴이 리포트하게 함. (비용 시나리오 추정을 실측으로 대체하려는 목적) · ## 변경 · `gemini.ts`: 스트림 `usageMetadata`(promptTokenCount·candidatesTokenCoun…
- **#90** [기능] feat(edu): Phase 0 — 생성 학습 메타(logicSummary·conceptTags)  
  ## 무엇 (교육 로드맵 Phase 0 · 키스톤) · 생성물 하나하나에 학습 메타를 붙인다.
- **#91** [기능] feat(edu): Phase 1 — 고치기를 '디버깅 대화'(기대/실제 2칸)로  
  ## 무엇 (교육 로드맵 Phase 1 · 최고 가성비) · '고치기'를 증상신고 → 디버깅 대화로 리프레임.
- **#92** [수정] fix(dev): CSP가 dev HMR(eval)을 막아 로컬 앱 전체가 죽던 문제  
  ## 증상 · 로컬 dev에서 모든 기능이 작동 안 함 — 게시판 빔, 로그인 버튼 안 뜸, 생성 버튼 무반응.
- **#93** [기능] feat(edu): Phase 1-b — /create 동작 필드 구조화(순서·조건·반복 3칸)  
  ## 무엇 (교육 로드맵 Phase 1-b) · 만들기(/create, 고학년 타깃)의 동작(how) 한 칸 → CT 유도 3칸으로 구조화.
- **#94** [기능] feat(edu): Phase 2 — 로직 설명 오버레이(logicSummary 카드)  
  ## 무엇 (교육 로드맵 Phase 2 · #1) · Phase 0에서 이미 생성·저장 중이던 `logicSummary`(저학년 쉬운말 ①②③ 로직)를 미리보기 위에 카드로 실제 표시.

### 2026-07-07
- **#95** [기능] feat(edu): Phase 2 — 개념 배지(conceptTags 표시)  
  ## 무엇 (교육 로드맵 Phase 2 · #2 개념 태그 — 파트 A만) · Phase 0에서 이미 생성·저장 중이던 `conceptTags`(순서·조건·반복·입력·출력)를 로직 카드에 아이콘 배지로 표시.
- **#96** [기능] feat(edu): Phase 2 — 핵심 논리 한 줄 발표(logicLine)  
  ## 무엇 (교육 로드맵 Phase 2 · #8) · 업로드 시 아이가 자기 말로 "내 작품의 핵심 한 줄"을 쓰고(선택), 게시판에서 AI 로직 카드 위에 인용 카드로 표시.
- **#97** [기능] feat(edu): Phase 2 — easy→create 브릿지('더 키우기')  
  ## 무엇 (교육 로드맵 Phase 2 · 브릿지) · 골라만들기(/easy)로 만든 걸 만들기(/create) 계획서로 열어 이어서 키우는 승급 경로.

### 2026-07-08
- **#98** [기능] feat(edu): Phase 2 — 확장 도전(AI 다음 도전을 고치기 칸 힌트로)  
  ## 무엇 (교육 로드맵 Phase 2 · #6, 옵션3) · 생성이 끝나면 AI가 "다음 도전 한 문장"을 제안하되, 새 버튼·카드 없이 고치기 '기대' 칸의 placeholder 힌트로만 노출.
- **#99** [기능] feat(edu): Phase 2 — 리믹스('한 가지 바꾸기' 프레임 + 원작자 크레딧)  
  ## 무엇 (교육 로드맵 Phase 2 · #7) · 이어만들기(포크)를 "그대로 복제"에서 "읽고 → 한 가지 의도적으로 바꾸기"로 프레이밍(이해→변형 = CT 전이).
- **#100** [기능] feat(edu): Phase 2 — 개념 수집 도감(마이페이지)  
  ## 무엇 (교육 로드맵 Phase 2 · #2B, 정적 도감) · 마이페이지에 "내가 모은 개념" 5칸(순서·조건·반복·입력·출력) — 지금까지 만든 작품들이 각 개념을 몇 개 작품에서 썼는지 모아 보여주고, 안 모은 개념은 흐리게 + 새 작품 도전 유도.
- **#101** [기능] feat(edu): 개념 '처음!' 축하(업로드 성공 옆에 작게)  
  ## 무엇 (교육 로드맵 · 도감 #2B 후속) · 업로드 성공 화면("게시판에 올라갔어요!") 아래에 이 작품에서 처음 써본 개념을 작은 배지로 축하 → 창 닫으면 넘어감.
- **#102** [기능] feat(edu): Phase 2 캡스톤 — 교사 학습 대시보드(개념 커버리지)  
  ## 무엇 (교육 로드맵 Phase 2 · 마지막 캡스톤) · 교사 콘솔(`/teacher`)을 운영 도구(학번발급·쿼터·신고)에서 형성평가 도구로 확장.
- **#103** [수정] fix(edu): 전체 점검 후 6건 수정(업로드 실패·집계 누락·크래시·UX)  
  ## 배경 · 세션에 추가한 교육 기능 13개를 4영역으로 나눠 적대적 코드 리뷰(병렬).

### 2026-07-10
- **#104** [기능] feat(edu): 개념별 '내 작품 예시' 노트(conceptNotes)  
  개념 배지를 누르면 일반 정의 아래에 '이 작품에선: …' 한 줄(그 프로그램에서 개념이 실제로 쓰인 방식)을 덧붙인다(하이브리드 C).

### 2026-07-12
- **#105** [기능] feat(edu): 골라서 만들기 역할 카드(선택 항목별 역할 A+B)  
  '내가 고른 것'의 각 선택에 역할 태그(A)를 붙이고, ⓘ를 누르면 역할 설명 + 개념 다리 칩(B)을 보여준다.

### 2026-07-14
- **#106** [수정] fix(edu·preview): detectConcepts 주석-따옴표 버그 + /share 오리진 폴백  
  전체 코드 점검(#1·#2) 수정. · ① detectConcepts stripJs 1패스 스캐너 (`lib/edu/detectConcepts.ts`) · 정규식 다패스가 `// don't` 같은 주석 속 짝없는 따옴표를 문자열 시작으로 오인해 다음 따옴표까지 코…
- **#107** [수정] fix(safety·perf): 계획서 본문 검열 + 스트리밍 재파싱 스로틀  
  전체 코드 점검 #3·#4. · ③ 계획서 본문 검열 (`lib/firebase/posts.ts`) — createPost/updatePostContent가 제목·이름만 검열하고 plan 본문(생김새·사용법·동작·기타, 공개·최대 5000자)은 미검열이라 정상 U…

### 2026-07-21
- **#108** [기능] feat(examples): 자동 예시 생성 루틴(교육테스트 보드)  
  놀고 있는 Gemini 무료 한도로 예시 작품을 교육테스트 게시판에 자동 생성.
- **#109** [수정] fix(examples): 크론 함수 504 타임아웃 방지 (시간 예산 하드 바운드)  
  ## 문제 · 자동 예시 크론(`/api/cron/generate-examples`)이 프로덕션 첫 자동 실행에서 `504 FUNCTION_INVOCATION_TIMEOUT`으로 실패(오늘 0개 생성).

### 2026-07-23
- **#110** [성능] perf(examples): 빠른 생성 모드(fast) — 예시 경로 thinking off  
  ## 배경 · 자동 예시 생성이 1건당 ~51초로 느려(thinking 지연) 크론의 시간예산(52s)에 자주 abort → 45콜 실측에서 성공률 ~53%(24/45)로 낭비가 컸음.

### 2026-07-24
- **#111** [기능] feat(ai): 생성 코드 실행 가능성 게이트 — 죽은 프로그램 게시 차단  
  ## 문제 · 게시된 자동 예시 52건 중 7건(약 13%)이 실행 불가였다.
- **#112** [기능] feat(ai): 코드 정확성 규칙 — 실행 불가 생성물을 애초에 줄인다  
  ## 배경 · 게시된 예시 52건 중 7건(약 13%)이 실행 불가였다. PR #111의 검증 게이트는 이를 '내보내지 않게' 막지만, 불량률 자체는 그대로다. 아이는 여전히 ~50초를 기다린 뒤 "다시 만들어 볼까요?"를 보게 된다. · 이 PR은 애초에 덜 만…
- **#113** [기능] feat(examples): 자동 예시 다양성 — 소재 시드 + AI 재량 확대 + 타입 중복 방지  
  ## 배경 · 자동 생성 예시가 단조로워 보이는 원인을 코드에서 확인했다. 이 PR은 B-1(예시 전용)만 다룬다 — `randomPlan.ts`는 실사용 설문(`/easy`)이 거치지 않는 경로라 아이들이 직접 만드는 작품에는 영향이 없다. (제목 상수화 문제 …

### 2026-07-25
- **#114** [기능] feat(landing): 네비 안내를 hover로도 즉시 노출 + 화살표 제거  
  ## 변경 · 메인 페이지 우측 네비(LandingNav)의 첫 행동 안내 두 가지 수정.
- **#115** [기능] feat(nav): 네비 라벨 글꼴 Jua + Dock 라벨 세로 중앙 정렬  
  ## 변경 · ① 네비 라벨 글꼴 Jua 적용 · 헤더 네비(`GlowNavLink`)와 랜딩 Dock 라벨의 글꼴을 본문 상속(Gowun Dodum) → 제목용 Jua(`font-display`)로 통일.
- **#116** [수정] fix(nav): 헤더 네비 라벨 글씨 0.5px 내려 아이콘과 광학 정렬  
  ## 변경 · Jua 적용 후 헤더 네비 라벨(만들기·골라서 만들기·게시판) 글씨 중심이 아이콘 중심보다 ~0.3px 위에 떠 보였다.
- **#117** [기능] feat(create): 예시 1클릭 입력 + 4종 정리(그림판 추가)  
  ## 변경 (만들기 탭 하단 예시 영역) · '이 예시 가져오기' 버튼 제거.
- **#118** [수정] fix(create): '어떻게 움직이나요?' 라벨 스타일 통일  
  ## 문제 · 만들기 폼에서 '어떻게 움직이나요?'(동작 섹션 헤딩)만 다른 필드 라벨과 스타일이 달랐다.
- **#119** [기능] feat(easy): 종류별 하위 카테고리 확장 — 게임 가위바위보·짝맞추기 외 9종  
  ## 변경 · ### 게임 — 비액션 장르 2종 추가 · 가위바위보(rps): 컴퓨터와 턴제 대결, 승/패/비김 조건 판정 + 누적 기록.
- **#120** [기능] feat(easy): '내 사진으로' 선택지 — 모습 단계에서 사진 발견 가능하게  
  ## 배경 · 사진 기능이 골라서 만들기 마지막 업로드 위젯에만 있어 아이가 존재를 모르고 지나치기 쉬웠다.
- **#121** [기능] feat(easy): '이름' 선택지 → 마지막 단계 이름 입력 반영  
  ## 배경 · '이름' 관련 선택지를 골라도 실제 이름을 받을 곳이 없어서, AI가 가짜 이름(placeholder)을 넣거나 아이가 완성 후 프로그램 안에서 직접 타이핑해야 했다.
- **#122** [기능] feat(easy): 역할 공백 메우는 선택지 5종 (게임 종료조건·수족관 성장 등)  
  ## 배경 — v1 리포트 검증부터 · `survey_options_review.txt`(v1)를 코드와 대조한 결과 약 60%가 부정확했다: · 사실오류: dressup을 "옷 입히기"로 서술(실제는 얼굴 꾸미기), quiz "힌트 단계" 있다고 함(없음), g…

### 2026-07-26
- **#123** [운영] chore(ci): 로컬 Claude 루틴 2개를 GitHub Actions로 이전  
  ## 배경 · 로컬 Claude 루틴은 Claude Code 앱이 켜져 있어야만 실행돼 예약 시각을 자주 놓쳤다 — `lun-auto-examples`는 새벽 5시 예정인데 마지막 실행이 오전 9:48(앱을 켠 시점)이었다.
- **#124** [수정] fix(security): 미리보기 CSP — 환경별로 sandbox/frame-ancestors 중 하나가 누락되던 문제  
  ## 발견 (실측) · 라우트 핸들러가 `Response`에 세팅한 CSP와 `next.config.ts` `headers()`의 CSP는 병합되지 않고 한쪽이 상대를 통째로 대체하는데, 어느 쪽이 이기는지가 환경마다 달랐다: · | 환경 | 이기는 쪽 | 실제 …
- **#125** [기능] feat(legal): 개인정보처리방침·이용약관·소개 + 만14세 가입 게이트  
  ## 배경 — 출시 블로커였음 · `/privacy`·`/terms`·`/about`이 전부 "내용을 준비하고 있어요" 껍데기인데 푸터에서 링크까지 걸려 있었다.
- **#126** [기능] feat(legal): 교사·학교 책임 분담 조항 + 학생 발급 시 보호자 동의 확인  
  ## 배경 · 개인정보처리방침에 "학교가 보호자 동의를 받는다"고 선언했지만 화면에 그 절차가 없어 실효가 없었다.
- **#127** [기능] feat(board,edu): 게시판 찾기(제목·개념) + 개념 도감 → 내 작품 연결  
  기능 리뷰의 ②게시판 검색·정렬과 ⑥도감↔작품 연결. 둘 다 *이미 저장하고 있는데 안 쓰던 데이터*를 탐색에 연결하는 작업이라 새 컬렉션·쓰기·인덱스·firebase 배포가 없습니다. · ## ① 게시판 찾기 · | | 방식 | 이유 |
- **#128** [기능] feat(board): 관리자에게 전체 게시판 노출 + 교실 보드 자물쇠 표시  
  ## 핵심: 권한 확대가 아니라 UI를 권한에 맞춘 것 · `firestore.rules`는 이미 관리자에게 교실 글 읽기를 허용하고 있었습니다.
- **#129** [기능] feat(admin): 게시판 관리에 주인·작품 수 표시  
  관리자 화면엔 여러 학교의 교실 보드가 함께 늘어서는데 이름만 보였습니다. "누구 반인지"도 "안에 작품이 있는지"도 모른 채 삭제·정리를 판단해야 했습니다. · ## 화면 · 각 게시판 이름 아래 한 줄이 붙습니다.

## 2026-08

### 2026-08-03
- **#130** [수정] fix(ai): 줄바꿈 없는 // 주석이 코드를 삼키는 결함 차단  
  자동 예시 생성이 하루 20~30건 → 1건으로 무너져 추적하다, 실사용 생성에도 영향을 주는 결함을 찾았습니다.

### 2026-08-11
- **#131** [기능] feat(seo): robots.txt·sitemap.xml + 메타데이터  
  커스텀 도메인(`eduai-lun.co.kr`) 검색 노출 준비. 구글/네이버 서치콘솔에 제출할 사이트맵 주소를 만듭니다. · ## 변경 · `app/robots.ts` → `/robots.txt`
- **#132** [기능] feat(seo): 네이버 서치어드바이저 소유확인 meta 태그  
  네이버 웹마스터도구 사이트 소유확인(HTML 태그 방식). · `layout` metadata.other에 `naver-site-verification` 추가 → `<head>`에 meta 태그로 출력 · 배포 후 네이버 서치어드바이저에서 확인 누르면 인증 완료 …

### 2026-08-14
- **#133** [수정] fix(deps): nanoid High 취약점 패치 (3.3.18)  
  8월 10일 「의존성 보안 감사」 워크플로 실패 원인. 경보 시스템이 새 취약점을 정상 검출한 것. · ## 취약점 · `nanoid` High 2건:
- **#134** [수정] fix(survey): 룰렛 한글 라벨 깨짐 — 범례 방식 유도  
  증상: 원형 룰렛 생성 시 한글이 지속적으로 깨짐(잘림·뒤집힘). · ## 원인 (실제 생성 코드로 확인) · AI가 원판 라벨을 canvas 회전 텍스트(`ctx.rotate` + `fillText`)로 그림:
- **#135** [수정] fix(auth): 학생 단일 세션 오발동 — 세션 id를 기기당 고정으로  
  증상: 일부 환경에서 학생 로그인이 저절로 풀리며 "다른 기기에서 로그인해서 이 화면은 로그아웃했어요" 배너가 뜸.
- **#136** [수정] fix(board): 교사가 자기 학급 보드에 못 올리던 문제  
  증상: 교사 계정으로 작품을 만들어 올릴 때 '올리기' 대화상자 카테고리 목록에 자기 학급 게시판이 안 나옴.
- **#137** [기능] feat(teacher): 학생 관리 편의 — 정렬·별명·일괄 삭제·일괄 한도  
  교사 콘솔 '우리 반' 목록의 학생 관리 편의 4종. · ## 1) 정렬 (버그 수정) · 학번 오름차순(`10101, 10102 …`). 기존엔 Firestore가 문서ID(랜덤 uid) 순서로 반환해 `110`이 `101`보다 앞서는 등 뒤죽박죽이었음. GET…
- **#138** [수정] fix(sound): 음소거 버튼이 간간히 안 먹히던 문제  
  증상: 소리 끄기(음소거) 버튼을 눌러도 일부 환경에서 소리가 계속 남. · ## 원인 · 재생 판정 `isSoundOn()`의 '진실값'이 localStorage뿐이었음. `setSoundOn()`은 `localStorage.setItem`이 실패하면 조용히 무…
- **#139** [기능] feat(moderation): 검열 차단 시 어느 칸·어떤 말인지 표시  
  문제: 게시판에 올릴 때 비속어로 막히면 어디에 나쁜 말이 있는지 몰라 못 고치는 경우가 있었음.

### 2026-08-18
- **#140** [기능] feat(seo): 한글 브랜드 음차(에듀에이아이룬·룬) 검색 연결  
  배경: `eduai lun`(영문)으론 홈이 구글 1위로 뜨지만, `에듀에이아이룬`·`룬 코딩` 같은 한글 음차는 사이트에 그 한글이 없어 검색이 연결되지 않았음.

### 2026-08-19
- **#141** [기능] feat(share): 공유 시 QR 코드 + 주소 함께 표시  
  요청: 공유하기 버튼으로 공유할 때 QR 코드와 주소가 같이 나오게. · 기존엔 공유 버튼이 주소를 클립보드에 복사만 하고 끝나 폰으로 바로 열기 어려웠음. 이제 누르면 QR + 주소 + 복사 버튼 팝업이 뜸. · ## 변경

## 2026-09

### 2026-09-01
- **#143** [기능] feat(privacy): 사진 첨부 시 국외 이전 별도 동의 체크박스  
  사진을 첨부하면 그 사진이 Google Gemini(미국)로 전송되므로(국외 이전), 첨부 전에 별도 동의를 받도록 `PhotoUpload`에 동의 체크박스를 추가했습니다.
- **#144** [기능] feat(ai): Gemini 티어 분리 — 사용자=유료(학습 미사용) / 자동생성=무료  
  사용자 데이터(아이 계획서·사진)는 항상 결제된 유료 티어로 보내 Google 모델 학습에 사용되지 않게 하고, 개인정보가 없는 내부 자동 예시 생성만 무료 티어를 쓰도록 키를 분리합니다.

### 2026-09-03
- **#145** [문서] docs: 아키텍처 및 핵심 알고리즘 문서  
  프로그램 전체 구조·데이터 모델·핵심 알고리즘 정리 문서(md + 가로 A4 PDF, Mermaid 다이어그램 6개).
- **#146** [기능] feat(board): 게시판 칩 + 단일 스크롤 + 트리 그룹화  
  게시판(카테고리)이 늘면 좌측 패널에서 게시물 목록이 밀려 사라지던 문제 해결.

### 2026-09-04
- **#147** [테스트] test: vitest 도입 — 순수 로직 4모듈 단위 테스트  
  아키텍처 점검에서 유일하게 '필요'로 판정된 항목. 순수 로직 4모듈에 38개 테스트. · `npm test` (vitest run, `lib//*.test.ts`, Firestore·Next 런타임 미사용) · validateCode / detectConcept…
- **#148** [CI] ci: 단위 테스트 단계 + 아키텍처 문서·PDF 갱신  
  ci.yml: tsc → npm test(vitest) → build. 시크릿 불필요. · 아키텍처 문서 §7 검증 관행에 단위 테스트·CI 흐름 추가, 헤더에 갱신일·커밋 해시. PDF 재생성(11쪽).

### 2026-09-07
- **#142** [법률] privacy: 법률 피드백 A그룹 반영 (국외이전 항목화·사진 별도동의·보유기간·자동수집)  
  법률 피드백 A그룹 반영본. 전문가 검토는 아직이지만 현행 문서보다 §28조의8 필수항목·보유기간·자동수집 고지가 갖춰져 있어 우선 게시하기로 결정(사용자 판단). B그룹(위탁계약·처리자 구조)은 전문가 검토 후 별도. · §1-라 자동수집 항목 · §5 국외이전…
- **#149** [운영] chore: 사이트 제목 변경 — LUN(룬) — AI 코딩  
  브라우저 탭·검색 결과 제목에서 '초등 저학년' 문구 제거. description·OG는 그대로.

### 2026-09-09
- **#150** [기능] feat(ai): 게이트 확장(미정의 이름·클래스 셀렉터) + 프롬프트 정확성 규칙  
  자동예시 899건 전수 품질 점검(정적+실행) 결과에 따른 조치. 보고서: docs/자동예시-품질점검-2026-09-09.md · validateCode: 정의 없는 이름 호출/참조, HTML에 없는 클래스 셀렉터 검출. 코퍼스 983건 재검사에서 오탐 0. · …
- **#151** [기능] feat(stats): 게이트 탈락 건수·사유 일일 통계  
  게이트 확장(#150) 후 오탐 감시용. 탈락 시 stats/{day}에 총건수·사유별 누적(사용자/자동예시 분리), daily-stats JSON에 gate 섹션 추가(rate = 탈락/(성공+탈락)). · 검증: tsc · vitest 44/44 · build…
- **#152** [수정] fix(ai): 게이트 정밀도 — 200건 실측 기반 오탐 제거  
  유료 키·일반 모드로 200건 생성 → 게이트 판정 + 샌드박스 실행(하네스) 교차 검증.
- **#153** [기능] feat(exemplars): 선택지 참고 예시 유형별 슬롯 12개  
  선택지 만들기(/easy)에서 유형(퀴즈·수족관…)에 맞는 참고 예시를 붙이도록 슬롯을 유형별로 확장.
- **#154** [기능] feat(ai): 미로 유형 제작 규칙(완전 미로 알고리즘 강제)  
  미로가 풀 수 없게 생성되는 문제. 서버가 선택지 유형별 제작 규칙을 시스템 프롬프트에 덧붙이는 층을 추가하고, 미로에 재귀 백트래킹 생성 함수를 그대로 쓰게 강제. · 실측 v1(검사 함수 포함): 10건 중 5건 멈춤/오류 — 모델이 검사 함수를 변형(`q.p…
- **#155** [기능] feat(ai): 꾸미기 좌표 고정 규칙 + 교육 문서 정리 커밋  
  꾸미기: 부품 기준점 좌표만 고정(그리는 방식 자유). 단위 테스트 갱신, tsc·build 통과. · docs: 그동안 미커밋이던 교육 가이드·지도안·시뮬레이션 기록·강사용 가이드·화면 캡처·예시 선별 기록 일괄 커밋(약 14MB).
- **#156** [운영] chore: 변경 이력 생성 스크립트 + 갱신 지침  
  docs/CHANGELOG.md(머지 PR 155건, 날짜별)와 재생성 스크립트(`npm run changelog`), CLAUDE.md 갱신 규칙.
