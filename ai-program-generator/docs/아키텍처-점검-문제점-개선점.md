# LUN 아키텍처 점검 — 문제점·개선점 (2026-09-03, 재판정)

> `아키텍처-및-핵심알고리즘.md` 기준으로 코드 실물을 대조해 정리한 뒤, "반드시 필요한가"를 다시 판정함.
> 결론: **필요 1개(테스트), 선택 1개(미리보기 오리진 검사)**. 나머지는 이미 해결됐거나 현 규모에서 이득 없음.

## 필요 (권장)
1. **자동화 테스트 부재** — 검증이 tsc+build+미커밋 selftest+수동. 순수 함수 `studentQuota`·`validateCode`·`detectConcepts`·`moderation` 4개를 vitest로 고정. 실제 회귀(개념감지 주석 삼킴, swallowed 주석)가 이 영역에서 있었음. 기능 추가가 계속될 때 값어치.

## 선택 (한 줄, 배포 없음)
7. `NEXT_PUBLIC_PREVIEW_ORIGIN` 이 앱 오리진과 같으면 격리 무효 → 빌드 시 동일 오리진 검사. 미설정 실패는 이미 있음.

## 이미 해결됨 (오판 정정)
2. 검색 누락 — 검색 중 자동으로 100건까지 스캔 + "더 찾아보기" 확장 있음. 조용한 누락 아님.
4. 카운터 핫스팟 — 조회는 `views/{uid}` create + `increment`(무경합). 트랜잭션 경합 없음.
8. 검열 우회 대응 문서화 — CLAUDE.md에 이미 명시.
9. `runtime = 'nodejs'` — generate route에 이미 선언.

## 불필요 (현 규모)
3. 세션 서버 강제 — 우회 비현실적, 비용 방어선(쿼터)은 서버가 지킴.
5. 레이트리밋 청소 통합 — 정크 수십 건, 영향 없음.
6. 크론 마지막 실행 시각 — daily-stats 리포트 부재·GitHub Actions 실패 메일로 이미 감지됨.
10~12. 문서 보강 — 있으면 좋음, 없어서 생기는 문제 없음.
